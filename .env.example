# Google OAuth credentials — isi dengan nilai Anda, jangan commit file .env
AGCTL_CLIENT_ID=
AGCTL_CLIENT_SECRET=

# Path browser untuk Puppeteer (opsional, auto-detect di Linux)
PUPPETEER_EXECUTABLE_PATH=

# Direktori data AG Manager (opsional, default: ~/.antigravity_tools)
AG_TOOLS_DIR=
